# Labeling workbench for cursor-predict

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Put tutorial videos in the repo `video/` folder. See the root README for the full workflow.
